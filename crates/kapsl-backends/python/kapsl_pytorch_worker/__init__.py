"""Kapsl PyTorch sidecar worker package.

Run as: ``python3 -m kapsl_pytorch_worker --model <path> --socket <path> --device <id>``.
See ``__main__.py`` for the protocol served to the Rust ``PyTorchBackend``.
"""

__version__ = "0.1.0"
