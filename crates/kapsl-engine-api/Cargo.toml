[package]
name = "kapsl-engine-api"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "1.0"
futures = "0.3"
serde = { version = "1.0", features = ["derive"] }
async-trait = "0.1.89"
base64 = "0.22"

[dev-dependencies]
serde_json = "1.0"
