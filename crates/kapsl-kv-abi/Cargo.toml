[package]
name = "kapsl-kv-abi"
version = "0.4.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Versioned backend-neutral KV-cache contract for Kapsl runtimes and inference engines"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"

[dev-dependencies]
serde_json = "1.0"
