[package]
name = "kapsl-quantization"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
safetensors = "0.3"
thiserror = "1.0"
half = { version = "2.3", features = ["serde"] }
log = "0.4"
anyhow = "1.0"
