[package]
name = "kapsl-managed-abi"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Versioned lifecycle and inference protocol for supervised Kapsl backend adapters"
readme = "README.md"

[dependencies]
kapsl-engine-api = "=0.3.0"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
